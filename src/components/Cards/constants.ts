export const SWAP_TYPE_PICKER_CARD_ID = (name: string) => {
  return `AWSwapTypePickerCard_${name}`;
};

export const FROM_SWAP_OFFERING_ASSET_BTN_ID = `AWFromSwapOfferingAssetButton`;

export const TO_SWAP_REQUESTING_BTN_ID = `AWToSwapRequestingAssetButton`;
